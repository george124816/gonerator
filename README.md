# uuid generator cli

## Installation

```bash
go install github.com/george124816/gonerator@latest
```

## Usage
```bash
generator uuid
```

After you install the package, make sure you add the directory where Golang keeps executable to your $PATH. If you installed Golang with asdf, you'll need to run `asdf reshim golang` once the executable is built.
